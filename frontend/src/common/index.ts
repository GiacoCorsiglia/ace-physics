/**
 * Anything that lives in this directory is common to both the frontend and the
 * backend.  Ideally this would be its own yarn workspace, but Create React App
 * doesn't support compiling TypeScript in files outside of the "src" directory.
 *
 * This can be imported from the backend via "ace-frontend/src/common/..".
 *
 * @see https://github.com/facebook/create-react-app/issues/1333
 */
export {};
