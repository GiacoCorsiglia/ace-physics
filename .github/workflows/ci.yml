name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: yarn
    - name: Check types
      run: yarn types
    - name: Lint
      run: yarn lint
    - name: Run tests
      run: yarn test
